[project]
name = "instant-ppt-workspace"
version = "0.0.0"
requires-python = ">=3.12.4,<3.13"
dependencies = [
  "instant-ppt-api",
  "instant-ppt-worker",
]

[dependency-groups]
dev = [
  "jsonschema==4.26.0",
  "pytest==9.1.1",
  "ruff==0.16.3",
]

[tool.uv]
package = false
link-mode = "copy"

[tool.uv.sources]
instant-ppt-api = { workspace = true }
instant-ppt-worker = { workspace = true }

[tool.uv.workspace]
members = ["services/api", "services/worker"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["services/api/tests", "services/worker/tests"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
