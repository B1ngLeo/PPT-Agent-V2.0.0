# G08 Windows screen-reader Gate

Status: **waiting for named human reviewer**. The current Windows host has no `nvda`
command and no NVDA executable in the standard Program Files or LocalAppData paths.

The reviewer must record:

- reviewer name and role;
- date/time and Windows build;
- exact Chromium browser version;
- exact NVDA (or other Windows screen reader) version;
- pass/fail and issue IDs for every step below.

## Required main-flow steps

- [ ] Sign in or enter the authenticated local test context and identify the page title.
- [ ] Use the skip link and navigate the product/header landmarks.
- [ ] Create a topic, choose a template and generate an outline without a mouse.
- [ ] Read and edit intent fields, story summary and at least one slide title/body.
- [ ] Approve the exact summary and start generation.
- [ ] Read the progressbar name/value and live terminal status without focus theft.
- [ ] Open the editable presentation, reorder a slide and request one-page regeneration.
- [ ] Trigger and understand the partial-export guard/recovery action.
- [ ] Export a bound revision, open history and restore the completed project.
- [ ] At browser zoom 200%, repeat the critical controls at 390, 768 and 1440 CSS px.
- [ ] Confirm focus visibility, dialog Escape/return focus, error announcements and that
      status is never conveyed by color alone.

## Sign-off

- Decision: pending
- Reviewer: —
- Checked at: —
- Browser/version: —
- Assistive technology/version: —
- Issues/results: —

Only the named reviewer may change this record and `GATE-G08-RELEASE` to passed, after
the separate Provider/privacy approval is also complete.
