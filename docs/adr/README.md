# Architecture decision records

ADR status is one of `proposed`, `accepted`, `superseded`, or `rejected`. Accepted ADRs are normative below `SPEC.md` and above non-normative design material. A decision that changes frozen P1 scope must also update `SPEC.md` and `PLAN.md`.

Use [ADR-000 template](ADR-000-template.md) for new decisions.
