# 即刻AI-PPT 产品文档

> 文档类型：产品说明 / PRD 摘要
> 适用版本：V2.1.0（2026-09-01）
> 产品阶段：本地单用户 P1 闭环基线
> 事实口径：当前代码和机器验收结果优先；历史 SPEC 中尚未落地或已变更的配置不作为现状承诺。

## 1. 产品摘要

即刻AI-PPT 是一款面向业务汇报、方案提案和内容策划场景的 AI 演示文稿产品。用户输入一个主题或上传一份主文档，系统先帮助用户确认目标、受众、故事线和逐页大纲，再由 Presentation Agent 异步创作页面，并通过内容、视觉和文件质量检查生成原生可编辑 PPTX。

产品不以“输入一句话后立即给出整套成品”为核心，而是以“减少方向性返工、控制长任务风险、保留编辑权和版本证据”为核心价值。

一句话价值主张：

> 先把故事讲清楚，再生成可编辑、可恢复、可追溯的 PPT。

## 2. 用户问题与产品机会

### 2.1 核心问题

用户通常只有一个主题、零散信息或长文档，却需要在较短时间内产出结构清楚、视觉一致、可继续修改的演示文稿。常见问题包括：

1. **目标理解偏差**：一键生成前没有确认受众、目的和结论，整套内容方向错误；
2. **信息组织困难**：用户知道材料内容，但不擅长取舍、建立故事线和拆分页面；
3. **过程不可控**：生成时间长，刷新或离开后进度丢失，不知道失败发生在哪一页；
4. **结果不可编辑**：整页图片或扁平化结果难以在 PowerPoint/WPS 中继续修改；
5. **失败成本高**：单页失败导致整套重来，重复生成增加等待时间和模型成本；
6. **版本不明确**：大纲、生成稿、人工修改和 AI 重生成互相覆盖，无法追溯；
7. **AI可信度不足**：来源、质量检查、降级方式和结果边界没有清晰披露。

### 2.2 产品机会

AI 的优势是理解语义、组织内容和动态设计；传统软件的优势是状态稳定、权限清晰和结果可验证。产品机会在于把两者组合成完整内容生产流程，而不是只提供一个 Prompt 输入框。

## 3. 产品定位

### 3.1 产品承诺

即刻AI-PPT 帮助用户完成以下闭环：

```text
创建草稿
  → 输入主题或上传文档
  → 确认创作意图
  → 编辑并批准逐页大纲
  → 确认设计授权与生成边界
  → 观察逐页生成和质量检查
  → 处理失败或部分结果
  → 轻编辑与单页重生成
  → 导出指定版本的原生 PPTX
  → 从历史继续工作
```

### 3.2 结果定位

生成结果统一定位为：

> AI 生成的可编辑初稿，请核验事实、数据、图表与图片。

产品保证流程、工件和技术质量边界，不承诺任意主题均能无需人工修改直接发布。

### 3.3 产品差异点

| 维度 | 常见一键生成 | 即刻AI-PPT |
|---|---|---|
| 需求确认 | 输入主题后直接生成 | 先确认 Intent、故事线和逐页 Outline |
| 创作方式 | 模型填充固定模板 | Main Agent 持有整套上下文并动态逐页创作 |
| 过程 | 黑盒等待 | 展示规划、逐页创作、检查、编译和发布状态 |
| 失败处理 | 整套重试 | 稳定页面 ID、失败槽位、单页重试和部分成功 |
| 编辑 | 常见为图片化或弱编辑 | 原生文本、形状和受支持对象可编辑 |
| 版本 | 覆盖式修改 | 不可变 Revision，可比较和恢复 |
| 质量 | 只判断是否生成文件 | 内容、SVG、视觉、PPTX 包和兼容性多层检查 |
| 降级 | 模型失败后不透明替换 | 模板回退独立标识，不冒充 Agent 结果 |

## 4. 目标用户

| 用户角色 | 核心任务 | 主要痛点 | 产品价值 |
|---|---|---|---|
| 业务负责人 | 制作经营汇报、策略方案、项目复盘 | 时间紧、逻辑分散、结论不聚焦 | 帮助建立结论先行的故事线并快速形成初稿 |
| 方案/销售人员 | 将客户材料转成提案 | 长文档取舍困难、需交付可编辑文件 | 从主文档提炼内容并输出可继续定制的 PPTX |
| 内容/品牌人员 | 保持结构与视觉一致 | 反复套版、样式漂移、版本混乱 | 使用稳定模板与跨页设计规范，保留版本历史 |
| 结果审核者 | 检查和修正 AI 结果 | 来源不明、失败不可恢复、导出版本不清 | 查看批准边界、失败状态、质量报告和明确 Revision |

### 4.1 典型使用场景

- 根据一个业务主题生成汇报结构和专业初稿；
- 将 DOCX/PDF/PPTX/HTML 材料整理为演示文稿；
- 对已有 AI 初稿做文字调整、排序和单页重生成；
- 在失败或刷新后继续长时间生成任务；
- 从历史项目恢复并导出指定版本。

## 5. 产品目标与范围

### 5.1 当前核心目标

1. 形成从主题/文档到可编辑 PPTX 的完整闭环；
2. 在正式生成前确认高成本、难回滚的内容与设计决策；
3. 使长任务可观察、可取消、可恢复；
4. 使单页失败不会破坏其他已完成页面；
5. 使 AI、人和模板产生的结果可以明确区分和追溯；
6. 建立内容、视觉、可编辑性、安全和兼容性验收基线。

### 5.2 当前范围

- 主题输入；
- 单个 DOCX、PDF、PPTX 或 HTML 主文档；
- 结构化 Intent 和 Outline；
- 内置模板选择；
- `agent-authoring` 原生专业模式；
- 逐页生成、SSE 进度、取消、失败页重试；
- 文本修改、页面排序、删除和单页 AI 重生成；
- 不可变版本、历史恢复、PPTX 和项目数据导出；
- 本地开发身份与预留的 OIDC 多租户数据模型；
- 可选图片治理合同，但当前本地默认关闭。

### 5.3 非目标

- PowerPoint 级自由画布；
- 任意旧 PPT 一键美化；
- 截图、图片或 PDF 完整反向还原为可编辑图层；
- 多文档合并与 URL 抓取；
- DOC、PPT、MD、EPUB 输入；
- 多人实时协作、评论和复杂版本合并；
- 公开分享链接、支付、会员和模板市场；
- PDF/图片导出；
- 整页位图式“视觉创意”模式；
- 对任意主题承诺固定生成时长或无需修改即可发布。

## 6. 产品设计原则

### 6.1 先低成本确认，再高成本生成

Intent、Outline 和设计授权位于正式生成前。用户先确认目标与结构，可以降低整套生成后方向错误的返工成本。

### 6.2 结构化工作台优先，聊天作为辅助

受众、目标、页数、大纲和页面内容都应是直接可编辑的结构化对象。AI 助手必须产生可比较的新 Revision，而不只是返回一段聊天建议。

### 6.3 状态必须真实

界面展示排队、运行、检查、部分成功、需要人工处理、失败、取消请求和取消完成等真实状态，不使用本地计时器模拟进度。

### 6.4 用户始终保留控制权

用户批准内容、决定是否生成、选择如何处理失败页，并指定要导出的版本。系统不得静默改写批准内容、丢弃必需资源或切换作者模式。

### 6.5 AI 能力与责任边界同时披露

Agent、Reviewer、确定性模板和用户编辑的责任不同，必须在状态、版本、报告和导出文件名中保持可识别。

## 7. 信息架构

产品包含以下主要界面：

1. **首页**：主题输入、文档入口、模板选择、历史入口和额度提示；
2. **来源状态**：上传、扫描、解析、失败原因和重试；
3. **创作工作台**：Intent、故事线、逐页 Outline、AI 修改和版本状态；
4. **生成确认**：批准版本、来源、模板、模式、图片范围和设计授权；
5. **生成监控**：整稿阶段、页面状态、质量摘要、取消和单页重试；
6. **演示编辑器**：缩略图、当前页预览、文本编辑、排序、删除和 AI 重生成；
7. **版本与导出**：Revision 历史、PPTX 导出、项目数据导出；
8. **历史创作**：草稿、生成中、部分成功、失败和完成项目的恢复入口。

## 8. 核心用户旅程

### 8.1 从主题创建

1. 用户在首页输入主题；
2. 选择一个当前可用的内置模板；
3. 点击“生成大纲”；
4. 系统创建草稿并异步推断创作意图；
5. 用户核对标题、受众、目标、页数、语言和视觉偏好；
6. 系统生成故事线与逐页大纲；
7. 用户编辑、增删、移动或让 AI 改写；
8. 用户批准明确的大纲 Revision；
9. 用户确认来源、模板、作者模式和设计授权；
10. 系统创建不可变生成快照并启动异步任务；
11. 用户观察逐页进度并处理异常；
12. 完成后进入编辑器，修改并导出指定 Revision。

### 8.2 从文档创建

1. 用户选择 DOCX、PDF、PPTX 或 HTML；
2. 文件上传至隔离区；
3. 系统复核真实字节并执行病毒扫描；
4. 扫描通过后解析结构化正文与素材信息；
5. 用户基于解析结果确认 Intent 和 Outline；
6. 后续流程与主题创建一致。

如果解析失败，用户可以重试或替换文件，草稿中的主题和其他配置不得丢失。

### 8.3 刷新与离页恢复

- 用户刷新或离开后，可从任务 URL 或历史创作恢复；
- 系统先加载已持久化的任务快照，再补齐缺失事件；
- 已完成页面不重复生成；
- 进度不会因重复事件倒退。

### 8.4 失败与部分成功

- 单页失败后其他页面继续；
- 失败页面保留稳定槽位和失败原因；
- 用户可仅重试该页、删除该页，或明确接受缺页；
- 默认不允许把未确认的缺页结果包装成完整成功；
- 视觉阻断项、必需图片失败或质量门禁失败时进入人工处理状态。

## 9. 功能说明与验收

### 9.1 首页与草稿

功能要求：

- 支持主题与文档双入口；
- 至少存在主题或主文档时才可继续；
- 主 CTA 使用“生成大纲”，避免误导为立即生成 PPT；
- 草稿自动保存并显示“保存中 / 已保存 / 保存失败”；
- 保存失败保留本地输入并提供显式重试；
- 历史草稿根据自身 ID 恢复，不依赖当前页面内存。

验收要点：

- 网络重试不创建重复草稿；
- 并发修改不覆盖已成功保存的版本；
- 刷新后恢复最新持久化状态。

### 9.2 来源上传

功能要求：

- 展示上传、扫描、解析三个阶段；
- 支持合法文件重试和错误说明；
- 不向用户暴露永久公开文件地址；
- 恶意、损坏、加密或不支持文件不得进入后续生成。

验收要点：

- 四种支持格式能够完成真实解析；
- 病毒、伪造 MIME、压缩炸弹和路径穿越被拒绝；
- 刷新和 API 重启后状态可恢复。

### 9.3 创作意图

Intent 包含：

| 字段 | 说明 |
|---|---|
| 标题 | 演示文稿主题 |
| 受众 | 决策者、客户、团队或自定义受众 |
| 目标 | 策略决策、经营复盘、培训、提案等 |
| 页数 | 当前产品范围 4～30 页，默认 12 页 |
| 语言 | `zh-CN` 或 `en-US` |
| 内容深度 | 结论先行、平衡、研究型 |
| 视觉偏好 | 数据优先、图像插画、极简等 |
| 补充要求 | 用户自定义约束 |
| 来源 | 已批准的解析工件 |

人工或 AI 修改都产生新 Revision。模型输出不符合结构时只做有界修复，仍失败则允许用户手工填写。

### 9.4 故事线与大纲

功能要求：

- 展示故事线摘要和有序页面列表；
- 每页包含稳定 ID、页面类型、标题、要点和可选来源；
- 支持新增、删除、移动、单页改写和整纲优化；
- 撤销/恢复通过版本实现；
- 至少保留一页；
- 改变目标页数时提示重新调整大纲。

验收要点：

- 页面移动后身份不变；
- AI 修改创建可比较版本；
- 已批准大纲不因后续编辑而变化。

### 9.5 生成前确认

确认摘要至少展示：

- Intent 和 Outline 的明确版本；
- 模板版本与生成模式；
- 来源数量及“无来源受限初稿”披露；
- 图片范围；
- Presentation Agent 读取批准上下文并形成设计规范的授权；
- 当前生成行为与成本边界。

用户批准后形成不可变 Snapshot。后续环境变量、模板升级或草稿修改不会改变运行中的任务。

### 9.6 Agent 生成

产品中的 AI 角色：

- Strategist：建立叙事结构和整套设计规范；
- Executor：在同一上下文中顺序创作每页；
- Reviewer：查看渲染图并提出结构化视觉问题；
- Supervisor：限制权限、预算和修复次数，并执行恢复与终止。

用户价值不是“看见 Agent 名称”，而是：

- 页面能根据语义选择不同表达，而不是只填固定正文框；
- 跨页叙事和视觉语言保持一致；
- 质量问题能够触发有界修复；
- 每页结果都能追溯到实际生成证据。

### 9.7 生成监控

界面需显示：

- 任务 ID、模板、作者模式和总体进度；
- 生成计划、逐页创作、质量检查、编译、发布阶段；
- 每页 pending、running、ready、retrying、failed、cancelled；
- 质量摘要和可处理问题；
- 单一“取消任务”入口；
- 刷新恢复说明。

关键文案必须使用“取消任务 / 取消请求中”，不使用产品未支持的“暂停 / 继续”。

### 9.8 结果编辑与版本

当前编辑范围：

- 修改页面标题与正文；
- 页面上移、下移；
- 删除页面；
- 输入 AI 单页修改要求并重生成；
- 查看不可变 Revision 历史。

不提供元素级自由拖拽、任意形状编辑和 PowerPoint 完整功能。单页重生成失败时，旧的可用版本继续可见。

### 9.9 导出与下载

- 用户必须导出明确 Revision；
- 导出前重新执行编译和包级 QA；
- Partial 默认被守卫，只有处理失败页或明确接受缺页后才能继续；
- 下载使用短期私有链接；
- 模板回退文件名包含“模板化受限初稿”；
- 支持导出项目数据，便于数据可携带和审计。

### 9.10 历史创作

历史需区分：

- 草稿；
- 规划中；
- 生成中；
- 部分成功；
- 需要人工处理；
- 失败/取消；
- 已完成。

进入项目后应回到正确上下文：工作台、监控页或编辑器，而不是统一回到首页。

## 10. AI可信与降级设计

### 10.1 来源边界

- 默认只使用已批准来源或明确的“无来源受限通用初稿”；
- 无来源时不把事实解释包装成已核实结论；
- 文档中的提示性文字只作为内容，不作为系统指令；
- 内部 source fragment 标识适合审计，不应成为最终观众的主要引用形式。

### 10.2 质量门禁

完整成功至少需要满足：

- 批准内容没有被静默丢失；
- Direct SVG 合法且没有阻断性溢出；
- 图表/数据关系在启用时通过检查；
- 最终视觉复核无 blocking；
- PPTX 包结构、文本和资源引用通过 QA；
- 发布对象、manifest 和 Revision 一致。

技术检查通过不等于内容一定优秀，因此仍需展示“可编辑初稿”提示。

### 10.3 模板降级

当运维明确启用 `deterministic-template` 时：

- 结果标记为模板化受限初稿；
- 不宣称由 Agent 创作；
- 不计入 Agent 成功率；
- UI、监控、manifest 和文件名保持一致披露；
- 运行中任务不因配置变化被切换作者。

Provider 故障本身不授权系统静默降级。

### 10.4 图片策略

当前本地默认 `image_scope=none`，图片生成关闭。未来启用时必须同时满足：

1. 用户显式选择封面或指定页；
2. 仅用于非证据型视觉表达；
3. 组织配额可用；
4. Snapshot 冻结图片配置；
5. Worker 运行时开关和密钥就绪；
6. 图片以独立可替换 PPTX 对象嵌入，不得用整页图片冒充可编辑页面。

必需图片失败时进入人工处理，不静默省略或替换 Provider。

## 11. 状态与用户文案

| 状态 | 用户含义 | 推荐操作 |
|---|---|---|
| queued | 任务已创建，等待执行 | 继续等待或取消 |
| running | 正在规划、创作或检查 | 查看逐页进度 |
| cancel_requested | 已收到取消请求，正在安全停止 | 等待终态 |
| partially_succeeded | 部分页面可用，部分失败 | 重试、删除或接受缺页 |
| needs_manual | 存在无法自动解决的视觉、内容或资源问题 | 根据问题说明人工处理 |
| succeeded | 已完成并发布可编辑初稿 | 进入编辑器 |
| failed | 未形成可用结果 | 查看稳定错误并重试 |
| cancelled | 已取消且没有半发布结果 | 返回工作台或重新生成 |

关键文案：

- 首页 CTA：“生成大纲”；
- 结果：“Agent 创作的 AI 可编辑草稿”；
- Partial：“部分页面需要处理”；
- 无来源：“受限通用初稿，事实内容未经外部核验”；
- 链接过期：“链接已过期，请重新生成下载链接”；
- 模板回退：“本版本由确定性模板降级链路生成，不计入 Agent 创作成功率”。

## 12. 响应式与无障碍

### 12.1 响应式

- `>=1200px`：约 70/30 双栏工作台；
- `768～1199px`：模板/页面两列，AI 助手为抽屉；
- `<768px`：单栏，步骤横向滚动；
- 390、768、1440px 为核心验收宽度；
- 浏览器 200% 缩放时主流程仍可操作。

### 12.2 无障碍

- 语义化 HTML、关联 label 和可见焦点；
- 触控目标至少 44×44 CSS px；
- 状态不只依赖颜色；
- 进度使用可访问的 progressbar；
- 状态摘要通过 `aria-live` 通知；
- 模态支持焦点锁定、Esc 关闭和关闭后焦点恢复；
- 键盘可完成创建、编辑、生成、重试和导出；
- 尊重 `prefers-reduced-motion`。

## 13. 产品指标体系

### 13.1 建议北极星指标

**有效演示完成数**：用户完成“大纲批准 → Agent生成 → 至少一次查看/编辑 → 成功导出明确 Revision”的演示数量。

该指标同时要求用户获得结果、结果可用且完成真实工作闭环，比单纯“生成次数”更能反映价值。

### 13.2 漏斗指标

```text
创建草稿
  → Intent 完成率
  → Outline 生成率
  → Outline 批准率
  → 生成任务启动率
  → 完整/部分生成率
  → 编辑器打开率
  → Revision 导出率
  → 历史回访率
```

### 13.3 AI质量指标

- Agent path coverage；
- 最终无 blocking 的演示占比；
- 单页重生成率与重生成后接受率；
- `needs_manual`、partial 和 fallback 率；
- 内容完整性、结论清晰度、视觉层级、信息密度、版式匹配和整套一致性的人工评分；
- 来源支持率和无来源受限初稿占比；
- PowerPoint/WPS 打开成功率和原生可编辑性。

### 13.4 体验与可靠性指标

- 创建到大纲完成耗时；
- 排队时间、首张预览时间、整套生成时长；
- 刷新恢复成功率；
- Worker 重试后重复发布/重复扣费次数；
- SSE 重连与事件回放成功率；
- 导出成功率和下载成功率；
- API p95、错误率和可用性。

### 13.5 成本与安全指标

- 每套、每页模型 token 和成本；
- 平均视觉审核与修复轮数；
- Provider 超时、限流和结构化输出失败率；
- 恶意文件拦截率；
- 跨租户拒绝和异常访问；
- 图片用量、失败和人工处理率。

### 13.6 当前可使用的验收基线

- 10/10 金样本通过；
- E2E-001～012 通过；
- PowerPoint/WPS 各 10/10 页打开，无修复提示；
- 30/30 跨应用视觉比较完成；
- 两组真实 Qwen 六页结果均无画布溢出并保持原生对象可编辑；
- 恢复与竞争场景连续 10 轮通过；
- API 性能基线 22,131 个样本、0 error，GET/write p95 为 72.113/80.873 ms。

这些是工程验收数据。当前没有真实线上用户量、留存、转化率、节省时长或 NPS 数据，不应将上述指标包装为业务增长结果。

## 14. 当前产品状态

### 14.1 已完成

- 主题/文档双入口；
- 安全上传与四格式解析；
- Intent、Outline、批准和不可变 Revision；
- 内置模板和生成前确认；
- 真实 Main Presentation Agent 创作路径；
- 确定性 Supervisor 与多模态视觉反馈闭环；
- 逐页进度、刷新恢复、取消、partial 和失败页重试；
- 原生 PPTX、轻编辑、单页重生成、版本与精确导出；
- 历史恢复、项目数据导出和删除；
- 多租户数据边界、安全、可观测性和发布 Gate。

### 14.2 当前限制

- 发布范围是本地单用户版本；
- 图片生成默认关闭；
- 原生图表默认关闭；
- 当前不执行开放网页研究；
- 结果仍可能存在信息密度偏高、结尾行动与前文不一致、版式重复等非阻断质量问题；
- 对外托管前仍需补齐生产身份、密钥管理、供应商与数据治理门禁。

## 15. 产品路线图建议

### P1.1：质量与真实用户验证

- 建立至少 20～50 个真实业务主题的质量集；
- 引入盲评与用户任务完成测试；
- 优化结论—行动一致性、来源可读性、信息密度和版式多样性；
- 建立用户对 Agent 候选、模板回退和人工编辑量的偏好数据；
- 测量从主题到可用初稿的实际时间节省。

### P1.2：表达能力增强

- 在完整 QA 下灰度启用原生图表；
- 增强表格、关系图和数据页表达；
- 将受控图片能力扩展到封面和少量抽象页面；
- 支持演讲者备注和更适合观众阅读的来源标注；
- 建立私有模板分析和版式角色映射。

### P2：多人和生产化

- 正式 OIDC、团队空间和角色权限；
- 评论、审批与组织模板治理；
- 生产 KES/KMS、供应商合规和数据保留策略；
- 额度、成本看板和套餐能力；
- 基于真实指标优化激活、导出和复用率。

## 16. 主要风险与应对

| 风险 | 产品影响 | 当前应对 |
|---|---|---|
| 模型幻觉或来源错配 | 用户误用错误事实 | 批准来源、无来源披露、内容 QA、结果初稿提示 |
| Agent 成本和延迟高 | 等待长、单位成本不可控 | 分阶段预算、P01 门禁、有界重试和修复 |
| 多轮修复无改善 | 无限循环、成本增加 | 稳定问题指纹、最佳版本恢复、最多 5 次审核 |
| 固定模板冒充 Agent | 用户预期失真 | 独立模式、UI/manifest/文件名显式披露 |
| 单页失败拖垮整套 | 返工和流失 | 页面级状态、partial、稳定 ID 和单页重试 |
| 刷新或 Worker 崩溃 | 用户认为任务丢失 | PostgreSQL 真相、SSE 回放、checkpoint 和 lease 恢复 |
| 输出不可编辑 | 无法进入真实办公流程 | Direct SVG、原生 PPTX 与 package QA |
| 图片承担事实表达 | 事实不可验证且不可编辑 | 只允许非证据型图片，当前默认关闭 |
| 数据与文件泄露 | 高严重度安全问题 | 租户隔离、私有对象、扫描、最小 Provider 数据和短期下载 |

## 17. 验收场景

| ID | 场景 | 预期结果 |
|---|---|---|
| E2E-001 | 主题→Intent→Outline→生成→导出 | 获得绑定明确 Revision 的可打开 PPTX |
| E2E-002 | DOCX/PDF/PPTX/HTML 上传 | clean 后解析，失败有稳定错误和恢复动作 |
| E2E-003 | 批准后继续修改大纲 | 运行 Snapshot 不变，新修改形成新 Revision |
| E2E-004 | Worker 在单页后崩溃 | 恢复且不重复发布或扣费 |
| E2E-005 | Redis 重启与 SSE 重连 | 通过数据库快照和事件回放恢复完整状态 |
| E2E-006 | 单页失败 | 其他页继续，任务 partial，可按 slideId 重试 |
| E2E-007 | 取消与发布竞争 | 只有一个合法终态，没有半发布工件 |
| E2E-008 | 排序后单页重生成 | slideId 稳定，旧版本在新版本通过前可见 |
| E2E-009 | Partial 导出 | 默认被守卫，处理或明确接受后才可导出 |
| E2E-010 | 跨租户访问 | API、SSE、对象和下载均拒绝且不泄露存在性 |
| E2E-011 | 移动端与键盘 | 390/768/1440px 和键盘主流程可完成 |
| E2E-012 | 历史恢复 | 不同状态项目回到正确工作上下文 |

## 18. 相关文档

- [项目说明](README.md)
- [产品与技术 SPEC](SPEC.md)
- [原型说明](designs/ppt-ai-mvp/prototype-spec.md)
- [产品技术方案](designs/ppt-ai-mvp/technical-solution.md)
- [技术文档](技术文档.md)
- [Presentation Agent ADR](docs/adr/ADR-012-presentation-agent-authoring.md)
- [Agent Runtime 问题与验收](docs/issues/ISSUE-003-default-agentic-profile-lacks-real-presentation-agent-runtime.md)
- [发布 Gate 报告](docs/release-gate-report.md)
