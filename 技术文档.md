# 即刻AI-PPT 技术文档

> 文档类型：系统技术说明
> 适用版本：V2.1.0（2026-09-01）
> 适用范围：本地单用户发布基线与后续工程演进
> 维护原则：当描述冲突时，以当前代码、机器可读合同和已批准 ADR 为准，其次参考 `README.md`、`SPEC.md` 与历史实施文档。

## 1. 系统概述

即刻AI-PPT 是一个“先确认意图与大纲，再异步生成原生可编辑 PPTX”的 AI 演示文稿系统。用户可从一句主题或一份 DOCX、PDF、PPTX、HTML 文档开始，经过结构化意图、大纲批准、设计授权、Agent 逐页创作、确定性质量检查、多模态视觉复核、编译发布和版本化编辑，最终获得可在 PowerPoint/WPS 中继续编辑的 PPTX。

系统的核心不是单次模型调用，而是将概率性的演示创作与确定性的工程治理组合：

- Main Presentation Agent 负责需要判断力的策略和页面创作；
- Python Supervisor 负责权限、预算、状态、恢复和发布门禁；
- 多模态 Reviewer 只读审阅渲染结果，不直接改稿；
- PostgreSQL 保存业务事实，Redis 只用于任务和事件加速；
- `ppt-master` 负责 Direct SVG 检查、渲染及原生 PPTX 编译。

## 2. 总体架构

```text
浏览器
  │
  ▼
Next.js Web / BFF
  │
  ▼
FastAPI API ───────────────► PostgreSQL
  │                              │
  │                              ├─ 领域数据 / Revision / Snapshot
  │                              ├─ Job / Slide / Event / Lease
  │                              └─ Transactional Outbox
  │                                      │
  │                                      ▼
  │                               Redis / Celery
  │                                ├─ 普通 Worker
  │                                └─ Agent Worker
  │                                      │
  │                                      ▼
  ├──────────────────────────────► Provider Gateway
  │                                      │
  │                                      ▼
  └──────────────────────────────► ppt-master / SVG / QA / PPTX
                                         │
                                         ▼
                                  MinIO 私有对象存储
```

### 2.1 组件职责

| 组件 | 主要职责 | 明确不负责 |
|---|---|---|
| `apps/web` | 产品界面、草稿工作台、生成监控、轻编辑、历史与导出交互 | 文档解析、模型密钥、重型渲染 |
| `services/api` | 身份鉴权、租户隔离、领域 API、SSE、任务状态与下载授权 | PPT 生成与源文件解析 |
| `packages/domain` | SQLAlchemy 模型、领域服务、状态机、幂等、配额、事务 Outbox | UI 与模型推理 |
| `packages/contracts` | OpenAPI、JSON Schema、错误码、状态机、fixtures 和生成的 TS 类型 | 业务实现 |
| `services/worker` | 文档扫描解析、规划任务、Agent 工作流、渲染、QA、编译和发布 | 公网业务 API 与用户会话 |
| `provider-gateway` | 集中访问文本 Provider，隔离外部密钥和请求策略 | 业务数据持久化 |
| `vendor/ppt-master` | Direct SVG 作者规范、检查器、渲染、SVG→PPTX 与模板能力 | 多租户、任务队列和产品 UI |
| PostgreSQL | 业务事实、不可变版本、任务状态、事件、审计和用量 | 队列加速 |
| Redis | Celery broker、短期事件分发和缓存 | 最终业务事实 |
| MinIO/S3 | quarantine、clean、tmp、published 私有工件 | 公开静态文件托管 |
| ClamAV | 上传文件病毒扫描 | 文档解析 |

## 3. 技术栈

| 层级 | 当前实现 |
|---|---|
| Web | Next.js 16、React 19、TypeScript、App Router |
| API | FastAPI、Pydantic、Uvicorn |
| 领域与持久化 | PostgreSQL 17、SQLAlchemy 2、Alembic、psycopg 3 |
| 异步任务 | Celery 5、Redis 7、Transactional Outbox、SSE |
| 文件与安全 | MinIO/S3、ClamAV、SHA-256、短期签名 URL |
| AI Provider | Qwen 为当前本地默认文本 Provider；Kimi 为可配置替代；OpenAI Image 为受控图片路径 |
| 演示引擎 | `ppt-master@v4.7.0`、Direct SVG、DrawingML/PPTX、python-pptx、Pillow |
| 合同 | OpenAPI、JSON Schema、AJV、openapi-typescript |
| 观测 | OpenTelemetry、Prometheus、结构化日志、审计记录 |
| 工程工具 | pnpm workspace、uv workspace、pytest、Ruff、ESLint、Docker Compose |

## 4. 仓库结构

```text
apps/web/                         Next.js 产品前端与 BFF
services/api/                     FastAPI API、鉴权、SSE
services/worker/                  Celery、Agent、解析、渲染与 Provider Gateway
packages/contracts/               OpenAPI、Schema、fixtures、状态机、TS 类型
packages/domain/                  领域模型、服务、迁移与持久化
vendor/ppt-master/                固定版本的演示生成引擎和工作流资产
designs/ppt-ai-mvp/               产品原型、设计说明与技术方案
docs/design/                      分阶段系统设计
docs/adr/                         架构决策记录
docs/evidence/                    自动化、人工验收与发布证据
docs/issues/                      重要问题、根因、修复和验收边界
scripts/                          合同、集成、安全、恢复与发布验证脚本
infra/                            观测、容器和环境配置
```

## 5. 端到端数据流

### 5.1 主题创建与规划

```text
主题输入
  → 创建 Draft
  → 异步推断 IntentSpec
  → 用户修改并生成 IntentRevision
  → 异步生成 OutlineSpec
  → 用户增删、排序、改写
  → 生成 OutlineRevision
  → 用户批准指定版本
  → 生成不可变 Approval
```

Intent 至少描述标题、受众、目标、页数、语言、内容深度、视觉偏好和来源引用。Outline 使用稳定 `outlineSlideId`，修改、撤销、恢复和 AI 优化都通过新增 Revision 完成，不原地覆盖历史内容。

### 5.2 文档上传与解析

```text
创建 UploadSession
  → 客户端按精确策略直传 quarantine
  → API 复核对象大小、MIME、SHA-256
  → ClamAV 扫描
  → clean 分区
  → 隔离 Worker 解析
  → SourcePackage / Markdown / 素材清单 / conversion profile
```

安全原则：

- 支持 DOCX、PDF、PPTX、HTML；
- 用户文件名只作为元数据，不进入对象 key；
- MIME、magic bytes、压缩比、嵌套深度和路径都必须校验；
- 扫描器不可用时 fail closed；
- 文档正文属于不可信数据，其中的指令不能覆盖系统指令；
- 只有 hash 与 clean decision 一致的对象才能解析。

### 5.3 生成快照

批准大纲后，API 在同一事务中创建：

- `GenerationSnapshot`：冻结 Intent、Outline、来源、模板、模型、作者模式、图片范围和视觉复核策略；
- `GenerationJob` 与逐页 `GenerationJobSlide`；
- 初始 `JobEvent`；
- 待分发的 `OutboxEvent`；
- 配额预占和幂等记录。

快照通过 canonical JSON 的 SHA-256 绑定。环境变量或草稿后续修改不得改变已运行任务。

### 5.4 Agent 创作与发布

```text
Approved Snapshot
  → 导入批准来源
  → Main Agent / Strategist 生成 design_spec.md
  → 设计授权与 spec_lock.md
  → Executor 创建 P01 Direct SVG
  → P01 强制门禁
  → 顺序创建 P02…Pn Direct SVG
  → 内容 / SVG / 图表检查
  → 渲染 PNG 与多模态视觉复核
  → Main Agent 定点修复
  → 最终 SVG 门禁
  → 编译 PPTX
  → Package QA
  → 发布工件与不可变 PresentationRevision
```

### 5.5 编辑与导出

发布后用户可以修改文本、移动或删除页面，并按稳定 `slideId` 单页重生成。任何成功修改都会产生新的 `PresentationRevision` 和 `SlideVersion`。导出时必须选择明确 Revision，Worker 会重新编译并执行包级 QA，再签发短期私有下载 URL。

## 6. Presentation Agent 设计

### 6.1 角色边界

| 角色 | 负责 | 不允许 |
|---|---|---|
| Main Agent / Strategist | 阅读批准上下文、形成整套叙事与设计规范 | 修改批准 Intent/Outline、任意联网或访问数据库 |
| Main Agent / Executor | 在同一上下文中顺序创作每页 Direct SVG、根据观察修复 | 按页拆给多个作者 Agent、绕过质量门禁、直接发布 |
| Supervisor | 工具白名单、预算、重试、取消、fencing、checkpoint、终止条件 | 替 Agent 做开放式页面设计 |
| Visual Reviewer | 读取渲染图、返回结构化问题和稳定问题指纹 | 直接写 SVG、修改内容或发布结果 |
| 确定性 QA | 检查内容、SVG、图表、包结构、可编辑性和资源引用 | 把技术通过等同于视觉质量优秀 |

采用单主 Agent 而非逐页多 Agent，是为了保持整套演示的叙事上下文、设计语言和跨页节奏。Reviewer 可以独立，但只提供意见，页面所有权始终属于 Main Agent。

### 6.2 受控工具

Agent 只可调用演示创作领域工具，例如：

- 读取批准的 Intent、Outline、来源与设计目录；
- 写入设计规范、当前页面 Direct SVG；
- 运行内容、SVG、图表与 package 检查；
- 渲染页面；
- 请求视觉复核；
- 完成或暂停当前阶段。

Agent 默认没有任意 Shell、数据库凭据、通用文件系统、浏览器会话、跨租户对象或任意网络访问。

### 6.3 预算与恢复

运行时强制执行：

- turn、输入/输出 token 和成本预算；
- soft/hard timeout；
- 每阶段最多 5 次尝试；
- 允许工具集合；
- 取消信号和 Worker fencing；
- hash-bound checkpoint；
- 模型回合、工具调用、观察和终止原因持久化。

每页作者收据把最终 SVG hash 绑定到实际模型 turn、tool call、observation 和预算消耗。相同幂等键恢复时，已确认的 Provider 使用量不重复结算。

### 6.4 视觉复核

视觉 Reviewer 基于最终 SVG 渲染图返回结构化 finding。自适应策略满足：

- zero blocking 时立即通过；
- 最多 5 次审核；
- 问题使用稳定指纹跨轮追踪；
- 连续两轮无改善或质量恶化时恢复历史最佳 SVG；
- 仍有 blocking 时进入 `needs_manual`，不得标记完整成功。

### 6.5 显式模板回退

`deterministic-template` 是独立作者模式：

- 不调用文本 Provider 或 Agent 工具；
- 不产生 Agent author receipt；
- 状态、manifest、指标、UI 与文件名均披露“模板化受限初稿”；
- 不计入 Agent 成功率；
- Provider 故障不会静默切换到该模式。

## 7. 状态、幂等与一致性

### 7.1 任务状态

产品级生成任务主要状态：

```text
queued → running → succeeded
                 → partially_succeeded
                 → needs_manual
                 → failed
        → cancel_requested → cancelled
```

状态与阶段分离。用户界面显示的阶段包括生成计划、逐页生成、整稿检查、编译、包级 QA 和发布；内部 Agent 工作流还包含设计规范、P01 门禁、最终 SVG、视觉复核等细粒度阶段。

### 7.2 稳定页面身份

- `outlineSlideId` 标识大纲页面；
- `slideId` 标识生成后页面；
- 数组下标和当前排序都不是身份；
- 单页重试或重生成必须保留 `slideId`；
- 新版本通过前继续展示旧的可用版本。

### 7.3 事务 Outbox

领域状态、业务事件与 Outbox 在同一数据库事务中提交。Outbox runner 再将任务投递至 Celery、将事件分发到 Redis。即使 Redis 丢失或 Worker 重启，也可以从 PostgreSQL 重新构建任务状态与事件序列。

### 7.4 幂等与租约

- API 写请求使用 `Idempotency-Key` 与请求体 hash；
- changed-body 重放返回冲突；
- Worker 使用 lease token、过期回收和 fencing 防止旧执行者提交；
- attempt 不进入逻辑幂等键；
- 对象上传后、数据库提交前崩溃时可按确定性 key/hash 对账；
- 取消与发布竞争只能产生一个合法终态。

## 8. 数据模型

主要实体按领域分组如下：

| 领域 | 核心实体 |
|---|---|
| 身份与租户 | `User`、`Organization`、`Membership`、`ServiceActor` |
| 权益与审计 | `Entitlement`、`UsageReservation`、`UsageLedger`、`AuditLog` |
| 来源 | `Source`、`UploadSession`、`SourceArtifact`、`Artifact`、`ArtifactDownloadGrant` |
| 草稿规划 | `Draft`、`PlanningJob`、`ProviderCall`、`IntentRevision`、`OutlineRevision`、`OutlineSlide`、`OutlineApproval` |
| 生成 | `GenerationSnapshot`、`GenerationJob`、`GenerationJobSlide`、`JobEvent`、`OutboxEvent`、`GenerationArtifact`、`GenerationPublication` |
| Agent 证据 | `WorkflowRun`、`WorkflowStageAttempt`、`WorkflowAgentTurn`、`WorkflowAgentToolCall`、`WorkflowCheckpointSet`、`WorkflowGateReceipt` |
| 编辑与导出 | `Presentation`、`PresentationRevision`、`SlideVersion`、`SlideRegenerationJob`、`ExportJob`、`DataExport` |
| 运维 | `ProjectCleanupJob`、`ObjectReconciliationRun`、`IdempotencyRecord` |

关键不变量：

1. 所有业务对象包含明确组织边界；
2. Approval、Snapshot、已发布 Revision 和 Artifact 不可原地修改；
3. 旧版本不因模板、模型或配置升级而变化；
4. 未通过发布门禁的结果不可创建完整成功的发布记录；
5. 工件保存内容 hash、大小、MIME、版本、来源和私有对象 key；
6. Redis 不拥有最终业务状态。

## 9. API 概览

统一前缀为 `/v1`。写接口普遍使用幂等键，异步操作返回 `202` 和任务/操作定位信息。

### 9.1 身份、权益与工件

- `GET /me/entitlements`
- `GET /me/usage`
- `POST /artifacts/{artifactId}:authorize-download`

### 9.2 上传与来源

- `POST /upload-sessions`
- `POST /upload-sessions/{id}:complete`
- `GET /sources/{sourceId}`
- `POST /sources/{sourceId}:retry-parse`

### 9.3 草稿、意图与大纲

- `POST /drafts`
- `GET/PATCH/DELETE /drafts/{draftId}`
- `POST /drafts/{draftId}/intent:infer`
- `POST/GET /drafts/{draftId}/intent-revisions`
- `POST /drafts/{draftId}/outline:generate`
- `POST/GET /drafts/{draftId}/outline-revisions`
- `POST /outline-revisions/{revisionId}:approve`
- `GET /templates`
- `GET /history`

### 9.4 生成与事件

- `POST /drafts/{draftId}/generation-jobs`
- `GET /jobs/{jobId}`
- `POST /jobs/{jobId}:cancel`
- `POST /jobs/{jobId}/slides/{slideId}:retry`
- `GET /jobs/{jobId}/events`

SSE 先返回 PostgreSQL snapshot，再依据 `Last-Event-ID` 回放事件，之后衔接实时分发。客户端按单调递增 `seq` 去重，状态不得倒退。

### 9.5 演示编辑与导出

- `GET /presentations/{presentationId}`
- `GET/POST /presentations/{presentationId}/revisions`
- `POST /presentations/{presentationId}/slides/{slideId}:regenerate`
- `GET /operations/{operationId}`
- `POST /presentations/{presentationId}/exports`
- `GET /exports/{exportId}`
- `POST /drafts/{draftId}:export-data`
- `GET /data-exports/{exportId}`

## 10. 安全与隐私

### 10.1 身份与租户

- 本地使用显式开发身份；生产必须接入 OIDC；
- API、SSE、Worker、对象 key 与下载授权都重复校验 `organization_id`；
- 跨租户访问统一返回 404，避免泄露资源存在性；
- Provider Gateway 使用独立内部 token，不复用公开 API 凭据。

### 10.2 文件安全

- 对象按 quarantine、clean、tmp、published 分区；
- 病毒、伪造 MIME、压缩炸弹、路径穿越、加密/损坏文档均在解析前拒绝；
- Worker 非 root、根文件系统只读、限制 CPU/内存/PID/临时盘/时长；
- 不允许文档内容触发任意工具、网络或代码执行。

### 10.3 Provider 数据边界

- 密钥仅存在于私有 Gateway 或明确授权的图片任务进程；
- 前端、日志、审计和持久化工件不得包含密钥；
- 只向 Provider 发送完成任务所需的最小内容；
- 不持久化模型思维链；
- 图片需用户显式选择、配额允许、Snapshot 冻结和运行时开关同时满足；
- 图片路径失败时进入 `needs_manual`，不静默换 Provider 或丢弃必需资源。

### 10.4 对象与下载

- MinIO bucket 不设置公开 policy；
- 发布对象启用服务端加密与生命周期治理；
- 下载前重新授权并签发默认 15 分钟的短期 URL；
- 签名 URL 不写入数据库或日志；
- 支持项目数据导出、删除和对象 reconciliation。

## 11. Provider 与运行配置

当前 `.env.example` 的关键默认值：

```dotenv
PLANNING_BACKEND=qwen
TEXT_PROVIDER=qwen
QWEN_MODEL=qwen3.8-flash
QWEN_REASONING_EFFORT=medium
QWEN_ENABLE_THINKING=true
QWEN_PRESERVE_THINKING=false

PRESENTATION_AUTHORING_MODE=agent-authoring
PRESENTATION_VISUAL_REVIEW_REQUIRED=true
PRESENTATION_NATIVE_CHARTS_ENABLED=false

IMAGE_GENERATION_ENABLED=false
IMAGE_MAX_PER_DECK=0
```

说明：

- Qwen 是当前网站规划和演示创作默认 Provider；
- Kimi 仍作为配置化替代；
- 无外部模型回归可显式使用 fake planning 与 `deterministic-template`；
- 图片能力默认关闭；仅修改 Provider 密钥不能让用户请求自动启用图片；
- 原生图表当前默认关闭，不应在产品文案中承诺已普遍可用。

## 12. 可观测性

日志、trace、指标与审计通过以下 ID 关联：

```text
request_id / organization_id / draft_id / source_id / snapshot_id
job_id / slide_id / export_id / provider_call_id / trace_id
```

重点指标包括：

- API 请求量、错误率、p50/p95/p99；
- 上传、扫描、解析成功率和耗时；
- 队列等待、首张预览、逐页和整套生成耗时；
- Worker 运行数、崩溃、超时、租约回收；
- SSE 连接、重连与回放；
- Agent turn/token/cost、工具成功率、修复次数；
- `needs_manual`、partial、fallback 和取消率；
- SVG、图表、PPTX package QA 与 PowerPoint/WPS 打开成功率；
- 跨租户拒绝、恶意文件与审计异常。

## 13. 质量保障与验收证据

验证层级：

1. Contract：OpenAPI、JSON Schema、状态机、错误码、fixtures；
2. Unit：领域规则、状态迁移、权限、幂等、Agent 工具；
3. Integration：PostgreSQL、Redis、MinIO、ClamAV、Celery、Provider fake；
4. Golden：source → SVG → QA → PPTX；
5. E2E：主题/文档、生成、编辑、导出、历史恢复；
6. Security：跨租户、恶意文件、日志脱敏、对象治理；
7. Compatibility：PowerPoint/WPS 打开与视觉比较；
8. Recovery：Worker kill、Redis restart、SSE reconnect、取消/发布竞争和对象对账。

当前归档基线包括：

- 10/10 金样本通过；
- E2E-001～012 通过；
- PowerPoint/WPS 各 10/10 页打开，无修复提示，并完成 30/30 视觉比较；
- 恢复与竞争场景连续 10 轮通过；
- 20 个虚拟用户、22,131 个样本下，GET p95 72.113 ms、写请求 p95 80.873 ms、0 error；
- 两组真实 Qwen 六页结果均可渲染、无画布溢出，并由独立文本和原生形状构成。

这些属于工程验收指标，不代表线上用户增长、内容正确率或无需人工修改的商业质量承诺。

## 14. 本地开发与验证

### 14.1 环境基线

- Node.js 24.18.1
- pnpm 11.19.0
- Python 3.12.4
- uv 0.12.5
- Docker Engine 29.5.2
- Docker Compose 5.1.4

### 14.2 启动

```powershell
corepack enable
corepack prepare pnpm@11.19.0 --activate
pnpm install --frozen-lockfile
python -m pip install uv==0.12.5
python -m uv sync --frozen
Copy-Item .env.example .env

docker compose up -d postgres redis minio clamav
python -m uv run --package instant-ppt-api alembic -c packages/domain/src/instant_ppt_domain/alembic.ini upgrade head
docker compose --profile runtime up -d --build
pnpm --filter @instant-ppt/web dev
```

访问 `http://localhost:3000`。默认 CORS 白名单不包含 `http://127.0.0.1:3000`。

### 14.3 健康检查

```powershell
Invoke-WebRequest http://localhost:8000/healthz
Invoke-WebRequest http://localhost:8000/readyz
docker compose --profile runtime ps
```

### 14.4 验证

```powershell
docker compose config --quiet
pnpm verify
```

可按风险分别运行 `verify:contracts`、`verify:web`、`verify:api`、`verify:worker`、`verify:integration`、`verify:golden`、`verify:e2e`、`verify:security`。

## 15. 当前边界与后续演进

当前可用但需要明确披露的边界：

- 发布状态是本地单用户基线，不等同于外部托管生产就绪；
- 对外、多用户、QA、预发布或生产部署前必须恢复 KES/KMS 等生产安全门禁；
- 结果定位为“AI 可编辑初稿”，事实、数据、图表和图片仍需用户核验；
- 原生图表和图片默认关闭；
- 不提供 PowerPoint 级自由画布；
- 不支持多人实时协作、URL 抓取、多文档合并和公开分享；
- 模板回退可靠但质量上限有限，必须保持显式披露；
- 真实模型结果已证明动态创作链路成立，但结论—行动一致性、信息密度、版式多样性和观众可读来源仍是后续质量方向。

## 16. 相关文档

- [项目说明](README.md)
- [产品与技术 SPEC](SPEC.md)
- [系统设计](docs/design/system-design.md)
- [Presentation Agent ADR](docs/adr/ADR-012-presentation-agent-authoring.md)
- [Agent Runtime 问题与验收](docs/issues/ISSUE-003-default-agentic-profile-lacks-real-presentation-agent-runtime.md)
- [发布 Gate 报告](docs/release-gate-report.md)
- [运行手册](docs/runbook.md)
- [回滚说明](docs/rollback.md)
